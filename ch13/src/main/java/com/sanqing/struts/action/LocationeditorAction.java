/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.sanqing.struts.action;

import com.sanqing.struts.form.LocationeditorForm;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;

/**
 * MyEclipse Struts
 * Creation date: 04-20-2008
 * <p>
 * XDoclet definition:
 *
 * @struts.action path="/locationeditor" name="locationeditorForm" input="/form/storage/locationeditor.jsp" scope="request" validate="true"
 */
public class LocationeditorAction extends Action {
    /*
	 * Generated Methods
	 */

    /**
     * Method execute
     *
     * @param mapping
     * @param form
     * @param request
     * @param response
     * @return ActionForward
     */
    public ActionForward execute(ActionMapping mapping, ActionForm form,
                                 HttpServletRequest request, HttpServletResponse response) {
        LocationeditorForm locationeditorForm = (LocationeditorForm) form;// TODO Auto-generated method stub
        try {
            PrintWriter out = response.getWriter();
            try {
                String oper = request.getParameter("oper");
                com.sanqing.bll.BLocation B_V = new com.sanqing.bll.BLocation();
                if (oper.equals("add")) {
                    B_V.LocationAdd(locationeditorForm);
                } else if (oper.equals("edit")) {
                    B_V.LocationEdit(locationeditorForm);
                } else if (oper.equals("delete")) {
                    B_V.LocationDelete(locationeditorForm.getLoca_id());
                }
                out.print(B_V.LocationConConvertToHTMLTable());

            } catch (Exception e) {
                out.print(e.getMessage());
            }

        } catch (IOException e1) {
            e1.printStackTrace();
        }
        return null;
    }
}